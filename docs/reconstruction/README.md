# Reconstruction records

This directory preserves imported [provenance](PROVENANCE.md), [notices](NOTICE.md) and the [file inventory](imported-files.json). References inside those records to original installers, Git LFS, `frontend/` and `research-archives/` describe the parent reconstruction, not a requirement or claim about the standalone Clawd distribution.

`pnpm-lock.yaml` and `pnpm-workspace.yaml` are historical inputs. Install Clawd Bot using its root `package-lock.json` and `npm ci`. Keep imported notices intact; use [source publication](../github-readiness.md) for this application's current boundary.

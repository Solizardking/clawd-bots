# Clawd Bot pets

The bundled [Clawd pet](clawd/README.md) includes its manifest, animation atlas,
and portable Codex package. `npm run build:ui` copies this directory into
`dist-ui/pets/`; runtime changes belong in the source assets and their consumer.

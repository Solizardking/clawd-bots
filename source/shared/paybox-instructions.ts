// PayBox skill text: MIT, MoonPay. See plugins/paybox/LICENSE.
export const PAYBOX_INSTRUCTIONS = "PayBox integration: use paybox_ tools for PayBox. Read server tool schemas exactly. Never re-submit a write to finish it; poll get_request using request_id. Pending approval/signature/confirmation is not success. In this desktop/headless host, supported operations sign via the official SDK only when the user configured its scoped signing key. MCP-only operations may require a PayBox browser signing window; do not claim that this app renders one. Return approval/manage URLs to the user. Never request raw wallet private keys or seed phrases.\n\n---\nname: paybox-get-started\ndescription: Understand what PayBox access the user has granted and how to widen it. Use when the user asks what their wallet can do, or when a tool is blocked by a missing credential, a spend limit, or a strict approval mode. Covers the grant model and the approval modes.\nlicense: MIT\n---\n\n# PayBox access, and what to do when it blocks you\n\nYour standing instructions already cover the launch flow and the signing flow. This skill is the\ngrant model underneath them \u2014 what \"granted\" means and what to do when an operation is refused.\n\n## Invoke this skill when\n\n- A tool returns an access-policy block: missing credential, spend limit, strict approval mode\n- The user asks what the wallet has access to, or wants to change it\n\n## Granted is not the same as owned\n\n`list_credentials` returns two sets: what this connector may use, and `ungranted` \u2014 credentials the\nuser owns but has not granted. **A credential that exists but is not granted is invisible to\noperations.** Most \"PayBox can't do X\" is an ungranted credential, not a missing capability.\n\n## Approval modes\n\nEach granted credential carries one:\n\n| Mode | Behaviour |\n|---|---|\n| `autonomous` | Signs without asking |\n| `iframe` | Needs a confirm inside the signing window |\n| `always_approve` | Needs the user's passkey in the PayBox app every time |\n\nA blocked operation is often a mode, not a permission \u2014 `always_approve` will keep returning\n`pending_approval` until the user approves in the app.\n\n## Widening access\n\n`request_account_change` takes `add` (grant from `ungranted`), `remove`, `create` (only when nothing\nsuitable exists), and `set_mode`. The `manage_url` it returns **changes nothing by itself** \u2014 the\nuser confirms in their own session with their passkey.\n\nA newly granted or created wallet is still not usable until the user connects its signing key in the\nPayBox window. `list_credentials` will show it granted before that, so granted status alone does not\nmean you can sign.\n\n## Never\n\n- Never ask for a private key, seed phrase, or passphrase. PayBox never needs one, and no legitimate\n  flow asks the agent for it.\n- Never surface raw internal fields \u2014 `refresh_token`, envelope bytes \u2014 to the user.\n\n\n---\nname: paybox-pay-and-swap\ndescription: Details for moving money with PayBox that the standing instructions do not carry \u2014 units, the checks to run before spending, and what a card authorization does not do. Use when asked to swap, bridge, send tokens, or pay a merchant.\nlicense: MIT\n---\n\n# Moving money: the details that bite\n\nYour standing instructions cover which tool to reach for and the Solana mint rule. This skill is the\npart that is easy to get wrong.\n\n## Before spending anything\n\n`get_portfolio` first, and check the balance covers the amount **plus fees** \u2014 not just the amount.\nSkipping it turns a clear \"you need to grant the Solana wallet\" into an opaque mid-flow failure.\n\n## Amounts are in smallest units\n\n`request_transfer` takes `amount` in wei, lamports, or the token's base units \u2014 never a decimal\nfigure. Omit `token` for the native asset; otherwise pass the ERC-20 contract (EVM) or SPL mint\n(Solana).\n\n## Read the recipient back\n\n**A transfer moves real funds and cannot be undone.** Read the destination address back to the user\nbefore they sign. This is the one mistake with no recovery, and confirming it costs a sentence.\n\n## A card authorization is not a payment\n\n`request_payment` authorizes a merchant-scoped, one-time virtual card. It does **not** submit\ncheckout or charge the merchant. On success, call `claim_payment_credentials` once if the card is\nredacted, use it at the merchant, and only say paid after the **merchant** confirms \u2014 not when the\nrequest reaches `success`.\n\n## A swap can park more than one transaction\n\nAn `approve` before the `swap` is normal. The window handles both; do not treat the first as the\nwhole operation.\n\n## Fiat funding is the user's step\n\n`get_buy_link` returns a checkout URL. Share it \u2014 the user completes it themselves. Only say funds\narrived after a balance read confirms them; `verify_solana_balance` proves a specific transaction\nlanded without waiting for indexing.\n\n\n---\nname: paybox-x402-services\ndescription: Judgement for paying x402 services \u2014 which tool to reach for, why a listing fails, and telling the user the cost before spending it. Use when a task needs a paid API or an endpoint returns HTTP 402.\nlicense: MIT\n---\n\n# Paying for services, without surprising the user\n\nYour standing instructions cover the plugin contract flow (`discover_plugins` \u2192 `get_contract` \u2192\n`use_plugin`) and the World tool order. This skill is the x402 judgement around them.\n\n## Which tool\n\n- `use_service` \u2014 you want the result, and PayBox should handle the 402 round-trip\n- `pay_x402` \u2014 the payment *is* the point, or you are driving the request yourself\n- `mode=\"probe\"` on `use_service` \u2014 one unpaid diagnostic request; proves nothing about paid success\n\n## Cost before spending\n\nx402 spends real money per call. **Tell the user what a service costs before paying**, and say so\nbefore a loop: \"I'll check five sources\" can mean five payments. Quote `plan.x402.amount_usd`, which\nis already in dollars \u2014 never convert the atomic amount yourself.\n\n## A failing listing is usually the listing\n\nThe catalogue is large and unvetted. Many entries are dead, on unsupported rails, or return\nnon-compliant 402s. When one fails, **try a different service rather than retrying the same one** \u2014\nretrying spends again for the same reason.\n\n## Read the contract, don't guess the schema\n\n`get_contract` is what tells you a plugin tool's input shape. Guessing it is the usual cause of a\nrejected `use_plugin` call. Same for a discovered service: follow its exact method, parameters, and\nbody rather than inferring from the summary.\n\n## Partial success is a real outcome\n\nAt terminal `success` or `error`, inspect both `output.value.payment` and `output.value.resource`. If\n`resource.ok=false`, check `payment.status` before telling the user anything \u2014 the payment may have\nsucceeded while the fetch failed. Do not retry blindly; another call may cost another payment.\n\n\n";
